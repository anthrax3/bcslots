1) user visits site
2) a creating new recieving address is put in the job queue, and user is assigned a guid G
3) page long polls until address is created with guid 
4) once address is shown, page long polls for callback
