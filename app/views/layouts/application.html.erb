<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "BcslotsRails" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body style="background-color:#FFF; background-image:url('<%= asset_path 'background.png' %>');">

    <div id="addressModal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h3 id="modalLabel"><%= yield :address %></h3>
      </div>
      <div class="modal-body" style="width: 50%; margin: 0 auto;">
        <div id="qrcode"></div>
      </div>
      <div class="modal-footer">
        <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
      </div>
    </div>

    <div id="provablyFairModal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h3 id="modalLabel">Provably Fair</h3>
      </div>
      <div class="modal-body" style="width: 80%; margin: 0 auto;">
        <table>
          <tr><td>add:</td><td> <input value="0" size="2" style="text-align:right; width:32px;"></td></tr>
          <tr><td>reel position: </td><td><span id="last_reel_position"></span></td></tr>
          <tr><td>secret: </td><td><span id="secret"></span></td></tr>
          <tr><td>sha512 hash: </td><td><span id="current_hash"></span></td></tr>
          <tr><td>next sha512 hash: </td><td><span id="next_hash"></span></td></tr>
      </table>
      </div>
      <div class="modal-footer">
        <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
      </div>
    </div>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">BcslotsRails</a>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <li><%= link_to "help!", "/path2"  %></li>
              <li><a href="#" data-toggle="modal" data-target="#addressModal"><%= @data[:address] %></a></li>
              <li><a href="#" id="provably_fair" data-toggle="modal" data-target="#provablyFairModal"><%= @data[:next_hash] %></a></li>
              <li><%= link_to "Link3", "/path3"  %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <%= yield %>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
